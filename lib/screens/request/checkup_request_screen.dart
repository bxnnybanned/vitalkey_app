import 'package:flutter/material.dart';

class CheckupRequestScreen extends StatelessWidget {
  const CheckupRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Check-up Request Screen')));
  }
}
